# doc_images/
Images:
- `pll_chip_banner.png` — 1600×900 landscape banner (blog / social header).
- `pll_chip_snapshot.png` — annotated final mixed-signal chip (analog + digital + IO ring).
- `PLLTOP4_D40_layout.png` — analog PLL hard macro (C2-final) **with the on-macro ÷40** (cap banks + PLL core + `CK_DIV40` output).
- `pll_dcc_layout.png` — digital control core.
- `pll_chip_final.png` — raw chip render (no annotation).
