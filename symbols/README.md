# symbols/
- `*.sym` — xschem symbols for the analog blocks (for use in a schematic hierarchy).
- `PLLTOP4_D40.lef`, `pll_dcc.lef` — the hard-macro abstracts (pins + blockages) used as the
  place-and-route "symbols" at chip level.
