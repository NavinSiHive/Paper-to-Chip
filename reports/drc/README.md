# reports/drc/
DRC reports. `final_chip.txt` / `drc_fullchip.txt` — geometric DRC (all residuals are foundry hard-IP-internal). `formal_drc0.txt` — **formal DRC 0** after metal fill (min-density) + the standard IO-cell/hard-macro waiver, LVS still matching.
